{
  "name": "@frogg/protocol",
  "version": "1.5.34",
  "description": "Frogg shared protocol schemas and wire types",
  "files": [
    "dist",
    "!dist/**/*.map",
    "README.md"
  ],
  "type": "module",
  "exports": {
    "./*": {
      "types": "./dist/*.d.ts",
      "default": "./dist/*.js"
    }
  },
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "clean": "node ../../scripts/ci/clean-package-dist.mjs",
    "build": "tsc -p tsconfig.json --incremental false",
    "build:clean": "npm run clean && npm run build",
    "prepack": "npm run build:clean",
    "typecheck": "tsgo --noEmit",
    "test": "vitest run",
    "generate:validators": "node scripts/generate-validation-aot.mjs",
    "watch": "concurrently --kill-others --names validation,tsc --prefix-colors green,yellow \"node scripts/watch-validation-aot.mjs\" \"tsc -p tsconfig.json --watch --preserveWatchOutput\"",
    "prebuild": "npm run generate:validators",
    "pretypecheck": "npm run generate:validators",
    "pretest": "npm run generate:validators",
    "generate:json-schema": "node scripts/generate-json-schema.mjs",
    "generate:rust": "npm run generate:json-schema && node scripts/generate-rust-types.mjs",
    "postbuild": "npm run generate:rust && npm run generate:text-match-fixtures",
    "generate:text-match-fixtures": "node scripts/generate-text-match-fixtures.mjs"
  },
  "dependencies": {
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^20.9.0",
    "typescript": "^5.2.2",
    "vitest": "^4.1.6",
    "zod-aot": "0.21.1"
  }
}
