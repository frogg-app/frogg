---
title: Environment variables
description: Environment variables read by the Frogg daemon, CLI and installers, and the variables Frogg sets for agents, terminals and workspace services.
sidebar:
  order: 2
---

Booleans accept `1`, `true`, `yes`, `on` and `0`, `false`, `no`, `off`. Environment
variables override [`config.json`](/docs/reference/configuration/); CLI flags override both.

:::note[Custom brands]
Custom brands use their configured prefix for every Frogg setting. For Acme, use
`ACME_LISTEN`, `ACME_HOME`, `ACME_PASSWORD` and so on. A branded daemon deliberately ignores
`FROGG_*` values; replace those entries in its service unit and `.env` files before upgrading.
Official Frogg continues to use `FROGG_*`.
:::

## Daemon: core

| Variable                | Default                   | Meaning |
| ----------------------- | ------------------------- | ------- |
| `FROGG_HOME`              | `~/.frogg`                  | State directory |
| `FROGG_LISTEN`            | brand bind on port `9999` (`0.0.0.0:9999` for `frogg`) | Listen target (`host:port`, port, socket or pipe) |
| `PORT`                  |                           | Port used only when neither `FROGG_LISTEN` nor `daemon.listen` is set; the bind host still comes from the brand |
| `FROGG_PASSWORD`          |                           | Plaintext password, hashed at startup. Overrides `daemon.auth.password`. Also read by the CLI as its credential |
| `FROGG_TRUST_LAN`         | `true`                    | Treat private-network clients like loopback. Ignored while claim mode is on |
| `FROGG_CLAIM_MODE`        | brand default (`false` for `frogg`) | Lock the LAN out and let the first client claim an unclaimed daemon. Overrides `daemon.auth.claimMode` |
| `FROGG_HOSTNAMES`         |                           | Comma-separated allowed hosts, or `true`. Legacy alias `FROGG_ALLOWED_HOSTS` |
| `FROGG_CORS_ORIGINS`      |                           | Comma-separated extra origins, or `*` |
| `FROGG_TRUSTED_PROXIES`   | `loopback`                | Comma-separated proxy IPs, `true`, or `false` |
| `FROGG_WEB_UI_ENABLED`    | `false`                   | Serve the web app. Installed services set it to `true` |
| `FROGG_WEB_UI_DIST_DIR`   | bundled                   | Serve another web build |
| `FROGG_PAIRING_BASE_URL`  | `https://pair.frogg.app`  | Pairing link base. Legacy alias `FROGG_APP_BASE_URL` |
| `FROGG_PRIMARY_LAN_IP`    | detected                  | LAN address advertised in pairing offers |

## Daemon: relay and service proxy

| Variable                            | Meaning |
| ----------------------------------- | ------- |
| `FROGG_RELAY_ENABLED`                 | Enable relay. Without an endpoint, relay stays inactive |
| `FROGG_RELAY_ENDPOINT`                | Relay `host:port`. Overrides `daemon.relay.endpoint` and locks the app's endpoint setting |
| `FROGG_RELAY_PUBLIC_ENDPOINT`         | Endpoint advertised to clients |
| `FROGG_RELAY_USE_TLS`, `FROGG_RELAY_PUBLIC_USE_TLS` | TLS for the relay. Default `true`; `false` only for a plaintext test relay |
| `FROGG_SERVICE_PROXY_LISTEN`          | Separate listener for workspace service URLs |
| `FROGG_SERVICE_PROXY_PUBLIC_BASE_URL` | Public base URL for service links |

## Daemon: logging and diagnostics

| Variable                                  | Default                | Meaning |
| ----------------------------------------- | ---------------------- | ------- |
| `FROGG_LOG_LEVEL` (alias `FROGG_LOG`)         | `info`                 | `trace`, `debug`, `info`, `warn`, `error`, `fatal` |
| `FROGG_LOG_FORMAT`                          | `json`                 | `json` or `pretty` |
| `FROGG_LOG_CONSOLE_LEVEL`, `FROGG_LOG_CONSOLE_FORMAT` |                |  |
| `FROGG_LOG_FILE_LEVEL`, `FROGG_LOG_FILE_PATH` | `info`, `$FROGG_HOME/daemon.log` |  |
| `FROGG_LOG_FILE_ROTATE_SIZE`, `FROGG_LOG_FILE_ROTATE_COUNT` | `10m`, `3` |  |
| `FROGG_GIT_TRACE_FILE`                      |                        | Append every git command the daemon runs to this file |
| `MCP_DEBUG`                               |                        | `1` logs MCP traffic |

## Daemon: git, providers and execution

| Variable                              | Default   | Meaning |
| ------------------------------------- | --------- | ------- |
| `FROGG_GIT_MAX_PROCESSES_PER_SECOND`    | `2048`    | Git spawn rate limit |
| `FROGG_GIT_MAX_PROCESS_CONCURRENCY`     | `8`       | Concurrent git processes (legacy alias `FROGG_GIT_CONCURRENCY`) |
| `FROGG_PROVIDER_REFRESH_TIMEOUT_MS`     | `120000`  | Provider catalog refresh deadline |
| `FROGG_EXECUTION_SERVICE`               |           | `1` opts in to independent execution: agent runtimes survive daemon restarts. Check with `frogg execution-status` |

Provider CLIs read their own variables (`ANTHROPIC_API_KEY`, `CLAUDE_CONFIG_DIR`,
`CODEX_HOME`, `OPENAI_API_KEY`, `PI_COMMAND`, `OMP_COMMAND` and others). The daemon passes
its environment to the agents it starts, so set them where the daemon runs.

[Provider accounts](/docs/agents-and-providers/providers/#provider-accounts-multi-sign-in) add
one such variable per launched agent, for that agent's process only: the provider's
config-directory variable, pointed at the account's directory. Gemini is the exception —
it has no config-directory variable, so a Gemini account sets `HOME` instead, again only for
that agent's process. The daemon and every other provider keep the real `HOME`.

## Daemon: updates

| Variable             | Default                                           | Meaning |
| -------------------- | ------------------------------------------------- | ------- |
| `FROGG_AUTO_UPDATE`    |                                                   | Overrides `daemon.autoUpdate.enabled` |
| `FROGG_INSTALL_DIR`    | `~/.local/share/frogg` (`%LOCALAPPDATA%\frogg` on Windows) | Versioned install root |
| `FROGG_RELEASES_API`   | `https://api.github.com/repos/frogg-app/frogg/releases` | Release discovery |
| `FROGG_RELEASE_BASE`   | `https://github.com/frogg-app/frogg/releases`       | Download mirror (`<base>/download/v<version>/<asset>`) |
| `FROGG_GITHUB_TOKEN`   |                                                   | Token for rate limits or a private repository. `GH_TOKEN`, `GITHUB_TOKEN` or `gh auth token` are tried after a 403/429 |

## Daemon: voice and Companion

| Variable                              | Meaning |
| ------------------------------------- | ------- |
| `FROGG_VOICE`                           | Master switch for voice features |
| `FROGG_VOICE_NOTIFICATIONS`             | Spoken agent alerts |
| `FROGG_DICTATION_ENABLED`, `FROGG_DICTATION_STT_PROVIDER`, `FROGG_DICTATION_LOCAL_STT_MODEL`, `FROGG_DICTATION_LANGUAGE` | Dictation |
| `FROGG_VOICE_MODE_ENABLED`, `FROGG_VOICE_LLM_PROVIDER`, `FROGG_VOICE_STT_PROVIDER`, `FROGG_VOICE_LOCAL_STT_MODEL`, `FROGG_VOICE_LANGUAGE`, `FROGG_VOICE_TURN_DETECTION_PROVIDER` | Voice mode |
| `FROGG_VOICE_TTS_PROVIDER`, `FROGG_VOICE_LOCAL_TTS_MODEL`, `FROGG_VOICE_LOCAL_TTS_SPEAKER_ID`, `FROGG_VOICE_LOCAL_TTS_SPEED` | Speech output |
| `FROGG_LOCAL_MODELS_DIR`                | Local speech model directory |
| `OPENAI_API_KEY`, `OPENAI_BASE_URL`, `OPENAI_STT_API_KEY`, `OPENAI_STT_BASE_URL`, `OPENAI_TTS_API_KEY`, `OPENAI_TTS_BASE_URL` | OpenAI speech credentials |
| `STT_MODEL`, `STT_CONFIDENCE_THRESHOLD`, `TTS_MODEL`, `TTS_VOICE` | OpenAI speech models |
| `FROGG_COMPANION_ENABLED`, `FROGG_COMPANION_BACKEND`, `FROGG_COMPANION_MODEL` | Companion |
| `ANTHROPIC_API_KEY`, `ANTHROPIC_BASE_URL` | Companion `api` backend only |

## CLI

| Variable          | Meaning |
| ----------------- | ------- |
| `FROGG_HOST`        | Default daemon for commands (`host:port`, `tcp://host:port`, `ssh://user@host`). Without it the CLI tries the local socket or pipe, then the configured listen address, then `localhost:9999` |
| `FROGG_PASSWORD`    | Password sent to the daemon |
| `FROGG_PAIRING_QR`  | `0` hides the QR code in `frogg auth pair` |
| `FROGG_AUTOSTART`   | `0` or `1`: answer the autostart question in non-interactive `frogg onboard` |

## Installers

`install.sh` (Linux and macOS):

| Variable              | Default                     | Meaning |
| --------------------- | --------------------------- | ------- |
| `FROGG_VERSION`         | latest release              | Release to install |
| `FROGG_INSTALL_DIR`     | `~/.local/share/frogg`        | Install root |
| `FROGG_BIN_DIR`         | `~/.local/bin`              | Where `frogg` is linked |
| `FROGG_RELEASE_BASE`    | GitHub releases             | Download base |
| `FROGG_BUNDLE_URL`      |                             | Exact bundle URL (plus its `.sha256`) |
| `FROGG_BUNDLE_FILE`     |                             | Install from a local bundle |
| `FROGG_NO_SERVICE`      |                             | `1` skips the systemd/launchd service |
| `FROGG_NO_MODIFY_PATH`  |                             | `1` leaves shell startup files alone |
| `FROGG_LISTEN`          | `0.0.0.0:9999`              | Service listen address |
| `FROGG_HOME`            | `~/.frogg`                    | Service state directory |
| `FROGG_HEALTH_TIMEOUT`  | `30`                        | Seconds to wait for the new daemon |

`uninstall.sh` also reads `FROGG_PURGE=1` to delete `FROGG_HOME`.

`install-docker.sh`:

| Variable             | Default                    | Meaning |
| -------------------- | -------------------------- | ------- |
| `FROGG_VERSION`        | `latest`                   | Image tag |
| `FROGG_IMAGE`          | `froggapp/frogg:$FROGG_VERSION` | Full image reference |
| `FROGG_HOME`           | `~/.frogg`                   | Host state directory |
| `FROGG_PORT`           | `9999`                     | Host port |
| `FROGG_BIND`           | `0.0.0.0`                  | Host bind address |
| `FROGG_WORKSPACE`      |                            | Host directory mounted at `/workspace` |
| `FROGG_PASSWORD`       |                            | Daemon password |
| `FROGG_CONTAINER`      | `frogg-daemon`               | Container name |
| `FROGG_NO_PULL`        |                            | `1` skips `docker pull` |
| `FROGG_UPDATE`         |                            | `1` is the same as `--update` |
| `FROGG_HEALTH_TIMEOUT` | `90`                       | Seconds to wait on update |

## Desktop app

| Variable                  | Meaning |
| ------------------------- | ------- |
| `FROGG_ELECTRON_UPDATE_URL` | HTTPS update feed override, for testing |

## Set by Frogg for child processes

Agents:

| Variable        | Value |
| --------------- | ----- |
| `FROGG_AGENT_ID`  | The agent's ID. CLI commands run inside an agent use it as the caller |
| `FROGG_AGENT_CWD` | The agent's working directory |

Terminals: `FROGG_TERMINAL_ID`, `FROGG_WORKSPACE_ID`, plus activity-hook variables.

Worktree `setup` and `teardown` commands:

| Variable                   | Value |
| -------------------------- | ----- |
| `FROGG_SOURCE_CHECKOUT_PATH` | The original checkout (alias `FROGG_ROOT_PATH`) |
| `FROGG_WORKTREE_PATH`        | The new worktree |
| `FROGG_BRANCH_NAME`          | Its branch |
| `FROGG_WORKTREE_PORT`        | A port reserved for the worktree |

Workspace services (`scripts` with `"type": "service"` in `frogg.json`):

| Variable                    | Value |
| --------------------------- | ----- |
| `HOST`                      | `127.0.0.1` when the daemon listens on loopback, otherwise `0.0.0.0` |
| `FROGG_PORT`                  | This service's port |
| `FROGG_URL`                   | This service's proxy URL |
| `FROGG_SERVICE_<NAME>_PORT`   | Port of every service in the workspace |
| `FROGG_SERVICE_<NAME>_URL`    | URL of every service in the workspace |

`<NAME>` is the script name uppercased, with other characters replaced by `_`.
