---
title: Providers
description: The agent CLIs Frogg runs — Claude Code, Codex, Copilot, OpenCode, Pi, Oh My Pi and ACP agents — and how to install, detect and configure them.
sidebar:
  order: 1
---

import { Screenshot, Steps } from "@/components/docs";
import appHostProvidersImg from "@/assets/docs/app/host-providers.png";

Frogg does not ship a model or an agent of its own. The daemon runs agent CLIs that you
install and sign in to on the daemon host. Each one is a **provider**.

<Screenshot src={appHostProvidersImg} alt="Host settings, Providers" />

## Built-in providers

| ID         | Name      | Runs                                   | Default mode | Enabled |
| ---------- | --------- | -------------------------------------- | ------------ | ------- |
| `claude`   | Claude    | `claude` (Claude Code, via the Agent SDK) | `auto`    | yes |
| `codex`    | Codex     | `codex app-server`                     | `auto-review` | yes |
| `copilot`  | Copilot   | `copilot --acp`                        | agent        | yes |
| `opencode` | OpenCode  | `opencode` server with a bridge plugin | set by OpenCode | yes |
| `pi`       | Pi        | `pi --mode rpc` (`PI_COMMAND` overrides) | none       | yes |
| `omp`      | Oh My Pi  | `omp` (`OMP_COMMAND` overrides)        | `full`       | **no** |

Any agent that speaks the [Agent Client Protocol](https://agentclientprotocol.com) can be
added as a custom provider; see [custom providers](/docs/agents-and-providers/custom-providers/).

## Install and sign in

Install the CLI and log in **as the user the daemon runs as**. Frogg uses each CLI's own
authentication and never asks for your provider credentials.

```sh
# examples; follow each vendor's instructions
npm install -g @anthropic-ai/claude-code && claude
npm install -g @openai/codex && codex login
```

The daemon finds binaries on its `PATH`. A service installed by `install.sh` inherits the
`PATH` of the shell that ran the installer. If you install a CLI later into a directory
that was not on that `PATH`, re-run the installer or point the provider at the binary with
a `command` override.

After installing, refresh:

```sh
frogg restart
frogg provider ls
```

```text
PROVIDER  LABEL     STATUS     ENABLED   DEFAULT MODE
claude    Claude    available  Enabled   auto
codex     Codex     available  Enabled   auto-review
copilot   Copilot   not found  Enabled   ...
opencode  OpenCode  not found  Enabled   default
pi        Pi        not found  Enabled   default
omp       Oh My Pi  not found  Disabled  full
```

## Diagnose

```sh
frogg provider diagnostic claude
```

It shows how the command resolved, the binary version, auth status (for Claude, the
output of `claude auth status`) and relevant environment. In the app, expand a provider in
**host settings → Providers**.

## Models, modes and thinking

Models are discovered from each provider at runtime:

```sh
frogg provider models claude
frogg provider models codex --thinking
```

Pick them per agent in the composer, or on the CLI with `--provider codex/gpt-5.5`,
`--model`, `--mode` and `--thinking`. Modes are covered in
[permissions](/docs/using-frogg/permissions/).

Some providers expose toggles in the composer: **Fast mode** (Claude, Codex), **Plan mode**
(Codex) and **Auto accept** (OpenCode and ACP agents).

## Enable, disable, reorder

Toggle providers in **host settings → Providers**, or in config:

```json title="~/.frogg/config.json"
{
  "agents": {
    "providers": {
      "omp": { "enabled": true },
      "copilot": { "enabled": false },
      "claude": { "command": ["/opt/claude/bin/claude"], "order": 1 }
    }
  }
}
```

Apply with `frogg reload`.

## Provider accounts (multi-sign-in)

A provider CLI keeps its sign-in in one config directory. Frogg can manage more than one of
those directories so you can switch between logins — a personal and a work Claude
subscription, for example — without signing out.

`claude` and `codex` support accounts today. `codex` uses `CODEX_HOME`, `~/.codex` and
`auth.json`, and shares its `prompts` folder. Other providers are declared in the manifest but
disabled until their directory layout is confirmed against the CLI; enabling one is a
`config.json` edit, and the daemon warns at startup when an unverified entry is switched on.

Creating an account makes `<home>/.claude-<name>` and symlinks the shared folders
(`commands`, `skills`, `agents`, `projects`, `sessions`, `shell-snapshots`) back to the
primary `~/.claude`, so commands, skills and history stay shared while credentials do not.
You pick which folders to link; a folder that does not exist in the primary directory is
skipped with a warning. On Windows, creating symlinks needs Developer Mode or an elevated
shell; links that fail are reported per folder and the account still works.

Account names are lowercase slugs (`[a-z0-9-]`, no leading, trailing or doubled hyphens),
because the name becomes the config directory's suffix. **Renaming** an account afterwards
only changes its label, so a rename accepts any single-line text up to 64 characters — and it
never moves or renames the directory, so the sign-in inside it survives. The provider's
implicit **Default** account can be renamed too.

Sign an account in by opening a terminal for it: the daemon launches the provider's
interactive login command with `CLAUDE_CONFIG_DIR` pointed at that account's directory, and
you run `/login` inside it. An account shows as authenticated once its credential file
(`.credentials.json`) exists.

**Sign out** deletes just the account's credential files inside its own config directory,
leaving the directory, the linked folders and everything else in it alone. It is refused for
an account that is not signed in.

An account, or several at once, can be **exported** as a portable bundle and **imported** on
another daemon, so a sign-in made on one machine does not have to be repeated. The bundle
contains live credentials in plaintext: treat it exactly like the credential file itself. The
daemon never writes it to disk — it is returned over the authenticated session only. Import
provisions a fresh config directory, writes the credential files readable only by the daemon
user, and refuses outright when an account id or name already exists rather than overwriting a
sign-in.

Deleting an account only removes it from Frogg's list — the config directory, and the
credentials in it, are left on disk for you to remove. An agent that was launched with an
account you later delete keeps working: its next launch quietly falls back to the provider's
default account and the daemon logs a warning.

### Which account an agent uses

The account is picked **per agent, when the agent is created**, and it sticks: the agent keeps
that account when it is resumed or rehydrated after a daemon restart. Changing an agent's
account means starting a new agent.

An agent can be launched with:

- **Default** — the provider's primary config directory (`~/.claude`), ignoring whichever
  account is marked active,
- any account configured for that provider,
- or no choice at all, which uses the provider's **active** account — the same daemon-wide
  behaviour as before, and what every existing agent keeps doing.

Resolution order for the provider's config-directory variable (`CLAUDE_CONFIG_DIR` for
Claude), highest first:

1. an explicit `agents.providers.<id>.env` in
   [`config.json`](/docs/reference/configuration/#agents),
2. the account chosen for that agent,
3. the provider's daemon-wide active account.

**Make active** therefore sets the default for agents that do not choose, and no longer
silently retargets every agent on the provider.

#### Restricting an account's models

An account can be limited to a subset of its provider's models — useful for keeping a
metered subscription off the most expensive model. The effective set an agent can select is
the provider's models intersected with the account's allowed list. No list means no
restriction; an empty list permits nothing and the account cannot start an agent.

The daemon enforces this, not the client: a model an account is not permitted to run is
rejected when the agent is created *and* when it is resumed, whatever a client sends.

#### Picking it in the composer

The composer shows an **account** control immediately to the right of the model selector. It
appears only when the provider actually has accounts — a provider with none, or one whose
accounts capability is off, shows the same row of controls as before.

The list is **Default** followed by every account for that provider. **Default** always means
the primary config directory, so it is never relabelled with the active account's name — that
account has its own row. Until you pick something, the control names the provider's active
account, because that is the account a new agent launches with when no choice is made; pick
**Default** explicitly to use the primary config directory instead. An account that is not signed in yet is shown greyed out with
*Not signed in* and cannot be chosen, rather than launching an agent that would fail to
authenticate.

The choice applies to the agent you are about to start. Adding, removing or signing in to an
account updates an open composer straight away — there is nothing to refresh.

Once the agent is running, the account control stays in the chat composer but is greyed out
and does not open: it names the account the agent runs as, including the active account it
resolved to when the agent was launched without a choice. Because the account is fixed at
launch, start a new agent to use a different one.

#### Seeing it in the sidebar

A session row shows a small account glyph beside its title when its agent runs under a
provider that has accounts. It is icon only, so the row's trailing slot keeps showing the diff
stat or timestamp; hover the glyph to read the account name. A session with several agents
reports the newest one, which is the agent the row's title and status already describe.

### From the app

**Host settings → Providers → a provider** opens that provider's sheet, and everything about
a sign-in lives inside it. The sheet has one **tab per account**, and the tab you are on
scopes the whole page: its usage, its defaults, the models it may run, its system prompt, its
nickname and colour, moving it to another host, and deleting it. Nothing about an account is
editable anywhere else in the app. The provider row at the top of the sheet is its own tab and
holds what is not about a single account: the model catalogue and uninstalling the provider.

The provider's implicit **Default** sign-in always gets a tab, even before the daemon has
stored anything about it. The tab strip is hidden on a daemon that does not report the
accounts feature.

<Steps>

1. **＋** at the end of the tab strip opens the add-account form. Type a name: the form
   validates it as you type and shows the slug and the config directory that will be created.
   Every shareable folder is checked by default; uncheck the ones this account should keep to
   itself.
2. **Sign in** opens a Frogg terminal running that provider's own login with the account's
   config directory applied — for Claude, run `/login` in it. Terminals belong to a session,
   so the action needs at least one session on the host; it is disabled when there is none.
   The tab flips to *Signed in* once the CLI's login finishes.
3. **Use this account** makes that account the default for agents that do not pick one in the
   composer.
4. **Danger zone** at the foot of the tab holds **Sign out** and **Delete**. Both ask first,
   and deleting tells you the config directory it left behind.

</Steps>

Provisioning warnings from the daemon — a folder it could not link, for example — are shown
next to the control that caused them, and a rejected name or duplicate keeps the form open
with what you typed.

#### Per-account preferences

Each account tab also carries preferences the daemon stores with the account and applies
whenever an agent runs as it:

- **Nickname** and **colour** — what the account is called and the colour of its monogram
  wherever it appears. The **Default** account can be renamed too; the rename only changes the
  label and never touches the directory.
- **New agent defaults** — the model and thinking level an agent starts on when it is launched
  as this account. Only models the account is allowed to run are offered, and picking a new
  model resets the thinking level to that model's own default.
- **Model access** — the same per-account restriction described above, edited on the account
  it applies to.
- **System prompt** — text appended to the system prompt of every agent launched as this
  account. The daemon appends it at launch, so it applies whatever client started the agent.

Preferences need a daemon that reports the `providerAccountPreferences` feature; the sections
are not rendered on an older one.

Claude and Codex have accounts enabled. Other providers are declared but off; turn one on with
`providerAccounts.<provider>.enabled` in
[`config.json`](/docs/reference/configuration/#provideraccounts) once its layout is
verified.

### Gemini: accounts through a per-account HOME

Gemini CLI is the exception. It has **no** environment variable for its config directory: it
always reads `~/.gemini`, derived from the home directory. A second Gemini sign-in therefore
works differently — each account directory becomes a synthetic **home** for Gemini's process
only, with the real config directory nested inside it at `<home>/.gemini-<name>/.gemini`.

Gemini accounts ship **off**. Enable them deliberately with
`providerAccounts.gemini.enabled` in
[`config.json`](/docs/reference/configuration/#provideraccounts), knowing the trade:

- Gemini runs with `HOME=<home>/.gemini-<name>`. Nothing else on the host is affected — not
  other providers' agents, not the daemon, not your shell.
- Only these entries are symlinked back from your real home, so they stay shared: `.npm`,
  `.npmrc` (Gemini is launched with `npx`, so without the npm cache every launch re-downloads
  the CLI), `.cache`, `.gitconfig` (commit identity), `.ssh` (git over SSH) and
  `.config/gcloud` (Vertex AI / application-default credentials).
- **Anything else Gemini or a tool it runs reads from your home directory will not be there.**
  A link target that does not exist on your host is skipped with a warning rather than
  failing.
- The **Default** account keeps your real home, so it behaves exactly as before.

**Sign in** opens a terminal equivalent to:

```sh
HOME="$HOME/.gemini-<name>" npx -y @google/gemini-cli@0.52.0
```

then pick a sign-in method inside it. The daemon logs a warning at startup while Gemini
accounts are enabled, naming this behaviour.

## Usage and limits

**Host settings → Usage** shows provider-reported quota where the provider exposes it
(for example Claude, Codex and Kimi). Frogg reads those credentials read-only; the CLIs own
token refresh.

The usage section of a chat's context-window meter reports the quota of the account that
agent runs as: for a provider with [accounts](#provider-accounts-multi-sign-in), Frogg reads
the credentials from that account's config directory rather than the primary one. On macOS
the Keychain fallback is used only for the primary directory, so an account whose credentials
file is missing shows as unavailable instead of borrowing another sign-in's figures. The
Host settings page still reports the primary directory.

## Provider environment

Agents inherit the daemon's environment plus anything you set per provider (`env` in
config) or per run (`frogg agent run --env KEY=value`). Common variables:
`ANTHROPIC_API_KEY`, `ANTHROPIC_BASE_URL`, `CLAUDE_CONFIG_DIR`, `CODEX_HOME`,
`OPENAI_API_KEY`, `OPENAI_BASE_URL`.

## Claude

Multiple Claude sign-ins are managed by
[provider accounts](#provider-accounts-multi-sign-in): **Host settings → Providers → Claude**,
one tab per sign-in. Each account gets its own `CLAUDE_CONFIG_DIR` under
`<home>/.claude-<name>`, keeping credentials, settings and history private while the folders
you chose stay symlinked to the primary `~/.claude`.

Frogg never signs an account in for you. **Sign in** opens a terminal with that account's
config directory applied, equivalent to:

```sh
CLAUDE_CONFIG_DIR="$HOME/.claude-<name>" claude
```

then run `/login` inside it.

:::caution[Removed in this fork]
Earlier releases let you declare an extra Claude account as a custom provider with a
`params.claudeAccount` profile in `config.json`. That mechanism has been removed in favour of
provider accounts. Such entries are now ignored — the daemon logs a warning naming them at
startup, and the providers they declare launch against the default Claude config directory.
Recreate the accounts in the provider's settings sheet and delete the stale
`agents.providers.<id>` entries from `config.json`.
:::
