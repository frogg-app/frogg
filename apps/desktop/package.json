{
  "name": "@frogg/desktop",
  "version": "1.3.1",
  "private": true,
  "description": "Frogg desktop shell (Tauri v2)",
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "build:bridge": "esbuild src/bridge.ts --bundle --format=iife --platform=browser --target=es2020 --minify --outfile=src-tauri/bridge.js",
    "dev": "node --import tsx ../../scripts/dev/branded-run.mts tauri dev",
    "build": "node --import tsx ../../scripts/dev/branded-run.mts tauri build",
    "build:win": "node --import tsx ../../scripts/dev/branded-run.mts tauri build --runner cargo-xwin --target x86_64-pc-windows-msvc --bundles nsis",
    "build:win:zips": "node ../../scripts/release/package-windows-zips.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "npm run build:bridge && node --test test/*.test.mjs && cargo test --manifest-path src-tauri/Cargo.toml",
    "test:harness": "npm run build:bridge && node --test test/*.harness.mjs"
  },
  "dependencies": {
    "@frogg/branding": "*"
  },
  "devDependencies": {
    "@tauri-apps/api": "^2.11.1",
    "@tauri-apps/plugin-dialog": "^2.7.3",
    "@tauri-apps/plugin-notification": "^2.4.0",
    "@tauri-apps/plugin-opener": "^2.5.5",
    "@tauri-apps/plugin-os": "^2.3.2",
    "esbuild": "^0.28.2",
    "typescript": "^5.9.3"
  }
}
